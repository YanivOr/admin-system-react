import React from 'react';
import styled from 'styled-components';
import { Sticker } from '../layout/Common';

const Wrapper = styled.div``;

const Posts = () => {
  return (
    <Wrapper>
      <Sticker>Posts</Sticker>
    </Wrapper>
  );
}

export default Posts;
